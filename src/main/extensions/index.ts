export { ExtensionsManager } from './extensionsManager';

// (async function () {
//   console.log('--------- TESTING EXTENSION ---------');
//   const extensionID = 'testing-extension';

//   const extensionsManager = new ExtensionsManager();

//   console.log('enabling extension');
//   await extensionsManager.enableExtension(extensionID);

//   console.log('processsing query');
//   const results = await extensionsManager.extensions[extensionID].processQuery('testin query');

//   console.log('results', results);
// })();
